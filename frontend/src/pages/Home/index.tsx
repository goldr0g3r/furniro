import React from "react";
import HeroSection from "../../components/hero";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <HeroSection />
    </div>
  );
};

export default Home;
